# Draft Card Preview

Run this from the project root:

```bash
php artisan draft:image
```

The command writes `nathan-aspinall.png` in this directory using the same renderer as the Discord draft announcement card.
